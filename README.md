# Creatorshop

Creatorshop is a Next.js 16 web app for creator–software barter deals, plus an internal atomic component library at `/design-system`.

## Development

```bash
npm install
npm run dev
```

Useful checks:

```bash
npm run lint
npm run build
```

See `PRODUCT.md` for product scope and `CLAUDE.md` for repository conventions.
